# Archive Index

- **260516-7eab-worktree-aware-path-resolution** — Extended repo-arg grammar with optional /<wt-name> suffix (e.g., 'hop outbox/feat-x cd') so every existing verb inherits worktree-awareness. Folds worktree visibility into 'hop ls --trees'. No DB, no hop.yaml changes.
- **260510-nzb0-auto-commit-on-sync** — Made 'hop sync' auto-commit dirty trees before pull/push (git add --all :/, then commit with generated message, then pull --rebase, then push). Replaces the user's xpush bash helper; pull/push subcommands remain unchanged.
- **260508-rdgf-add-hop-push-subcommand** — Added 'hop push <name-or-group>' subcommand mirroring hop pull/sync. Publishes commits without re-pulling. Reuses runBatch, resolveTargets, and per-repo status-line conventions from the pull/sync pair.
- **260507-xj3k-add-pull-sync-subcommands** — Added 'hop pull <name>' and 'hop sync <name>' top-level subcommands. Pull = git pull (fetch+merge); sync = git pull --rebase then git push. Accepts repo name, group name, or --all flag for batch operations across cloned repos.
- **260507-q437-config-print-subcommand** — Added 'hop config print' subcommand that prints the resolved hop.yaml to stdout. Single-purpose verb that lets users view the active config without 'cat $(hop config where)' shell composition.
- **260507-9lk0-tighten-bare-form-binary-error** — Unified repo-verb grammar to 'hop <repo> <verb>'. Dropped 'hop cd <name>' and 'hop where <name>' top-level subcommands — cd/where become verbs at $2. Bare 'hop <name>' = shim cd / binary error. Clean v0.x break, no backwards compat.
- **260507-0cjh-add-open-verb** — Added 'hop <name> open' verb that shells out to wt's context-aware open command (VSCode, Cursor, terminals, file managers, etc.). Hop chdirs into the repo then exec's wt; cd round-trip via WT_CD_FILE temp. Declares wt as Homebrew dependency.
- **260506-koa2-flip-to-repo-first-grammar** — Flipped two-arg grammar so repo name always lives in $1 (e.g., 'hop outbox code' instead of 'hop code outbox'). Removed 'hop open' subcommand in favor of platform-native tool-form. Clean break, no compatibility shim.
- **260506-ceh2-config-scan-populate-repos** — Added 'hop config scan <dir>' subcommand to populate hop.yaml from on-disk repos. Auto-derives group assignment (convention vs explicit dir), defaults to print mode with --write for atomic merge. Routes through yamled for comment preservation.
- **260503-us8o-hop-rename-and-grouped-schema** — Renamed binary repo→hop, restructured config to grouped schema (named groups with provider/dir/repos), added bare-name dispatch with h shim, -C exec-in-context flag, and ad-hoc URL clone with auto-registration. Renamed path→where subcommand.
- **260503-iu93-bootstrap-go-binary** — Working v0.0.1 Go rewrite of the bash repo script with cobra CLI, embedded starter repos.yaml, internal/config + internal/repos + internal/proc + internal/platform packages, and 4 reference specs in docs/specs/. Release pipeline carved out to a separate change.
- **260503-dgq0-release-pipeline** — Cross-platform release pipeline mirroring run-kit's hand-rolled GitHub Actions workflow, with tag-driven version source. Publishes v0.0.1 GitHub Release with 4 binaries and updates sahil87/homebrew-tap for brew install.
- **260504-yr9l-complete-repo-names-second-arg** — Rejected in favour of 260506-koa2-flip-to-repo-first-grammar. The flip moves repo names to $1 where completion already works, dissolving yr9l's premise.
