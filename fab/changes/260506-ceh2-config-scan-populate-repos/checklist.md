# Quality Checklist: hop config scan — populate hop.yaml from on-disk repos

**Change**: 260506-ceh2-config-scan-populate-repos
**Generated**: 2026-05-07
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-fff at the tasks stage.

  This checklist validates that the IMPLEMENTATION matches the SPEC.
  Items are derived from:
    1. spec.md — every requirement should have coverage
    2. Plan decisions — technical choices should be reflected in code
    3. Project constitution — project-wide quality standards
-->

## Functional Completeness
<!-- Every requirement in spec.md has working implementation -->
- [ ] CHK-001 Subcommand placement: `hop config scan` is registered as a cobra subcommand under the existing `config` parent (alongside `init` and `where`) via a new `newConfigScanCmd()` factory in `src/cmd/hop/config.go`; not added as a top-level subcommand (Constitution VI).
- [ ] CHK-002 Argument validation: subcommand uses `cobra.ExactArgs(1)`; `<dir>` is normalized via `filepath.Clean` → `filepath.EvalSymlinks` → `os.Stat`; non-directory or EvalSymlinks-failure produces stderr `hop config scan: '<dir>' is not a directory.` (verbatim user input) with exit code 2.
- [ ] CHK-003 Flags defined: `--write` (bool, default false) and `--depth N` (int, default 3); `--depth < 1` produces stderr `hop config scan: --depth must be >= 1.` and exit code 2.
- [ ] CHK-004 `hop.yaml` precondition: subcommand calls `config.Resolve()` before walking; on missing config, emits the two-line scan-specific stderr (`no hop.yaml found at <ResolveWriteTarget()>` + "Run 'hop config init' first…") and exits 1; no walk or `git` invocations occur.
- [ ] CHK-005 Exit code mapping: 0 on success (incl. zero repos found); 1 on missing `hop.yaml`, YAML write failure, `git` missing, load error; 2 on usage errors (missing arg, dir validation, `--depth < 1`).
- [ ] CHK-006 Stdout/stderr conventions: print mode emits rendered YAML to stdout and summary to stderr; write mode emits empty stdout and summary + `wrote: <path>` trailer to stderr; matches `hop clone` precedent.
- [ ] CHK-007 `internal/scan` public API: package exports `Found{Path, URL}`, `Skip{Path, Reason}`, `Options{Depth, GitRunner}`, and `Walk(ctx, root, opts) ([]Found, []Skip, error)` exactly as specified; lives at `src/internal/scan/`.
- [ ] CHK-008 DFS algorithm and depth handling: stack-based DFS with `(path, depth)` entries; root at depth 0; `--depth N` is inclusive (examines `R`, `R/*`, …, `R/*^N`); registered repos are not descended into.
- [ ] CHK-009 Repo classification rules: classifier applies in order — worktree (`.git` is regular file), submodule (`.git` dir + ancestor is repo), normal repo (`.git` dir, pick `origin` else first remote), bare repo (`HEAD` + `config` + `objects/` + no `.git`), plain dir (recurse). First match wins; classified dirs are not descended into.
- [ ] CHK-010 Symlinks and loop detection: walker follows symlinks; maintains visited set keyed by `(device, inode)`; loop-encountered dirs skipped silently (no `Skip` entry); `Found.Path` is `filepath.EvalSymlinks` resolution.
- [ ] CHK-011 Git invocation contract: all `git` calls go through `internal/proc` (e.g., new `RunCapture(ctx, dir, name, args...)` helper); each invocation uses `context.WithTimeout(ctx, 5*time.Second)`; `git` missing → stderr `hop: git is not installed.` exit 1, checked lazily.
- [ ] CHK-012 Convention check: for each `Found{Path, URL}`, compute `convention := filepath.Join(repos.ExpandDir(cfg.CodeRoot, ""), DeriveOrg(URL), DeriveName(URL))`; on `filepath.Clean` equality, assign to `default` flat group (URL only, no per-repo `dir:`).
- [ ] CHK-013 Invented group naming (slugify): non-convention paths derive group from `filepath.Base(filepath.Dir(Path))` via the 5-step slugify (lowercase → replace runs of non-`[a-z0-9_-]` with `-` → trim → ensure leading `[a-z]` via `g` prefix → empty → CLI-layer skip line); result conforms to schema regex `^[a-z][a-z0-9_-]*$`.
- [ ] CHK-014 Per-parent-dir granularity: each distinct (canonicalized) parent dir produces one invented group; distinct parent dirs are not collapsed even if slugify outputs textually match.
- [ ] CHK-015 Group dir rendering: invented group `dir:` is canonical parent path with `$HOME` substituted to `~/...` if the path begins under `$HOME`; otherwise absolute path verbatim.
- [ ] CHK-016 Conflict resolution: existing-slug + matching dir → reuse; existing-slug + differing dir → suffix `-2`/`-3`/… and emit `note:` stderr line; intra-scan slugify collisions → first wins, second suffixed with same `note:`.
- [ ] CHK-017 Render path symmetry: print and write modes share the same in-memory render via `internal/yamled` (e.g., `RenderScan` for print; `MergeScan` for write); print emits to `cmd.OutOrStdout()`; write atomically replaces via temp+rename.
- [ ] CHK-018 Print mode header: rendered YAML is prepended with the exact two-line comment `# hop config — generated by 'hop config scan <user-arg>' on <YYYY-MM-DD> (UTC).` + `# Run with --write to merge into <resolved-hop.yaml-path>.`; date computed via `time.Now().UTC().Format("2006-01-02")`; `<user-arg>` is verbatim user input.
- [ ] CHK-019 Group ordering in output: existing groups in source order, then `default` (in original slot if pre-existing; else after existing groups), then invented groups sorted alphabetically; scan-contributed URLs appended within each group.
- [ ] CHK-020 Stderr summary block: both modes emit summary header `hop config scan: scanned <user-arg> (depth N), found <K> repos.` + indented breakdown lines (matched/invented/skipped); write mode appends `wrote: <path>`; print mode appends `Run with --write to merge into <path>.`; zero-counts may be elided.
- [ ] CHK-021 `MergeScan` signature: `internal/yamled` exports `MergeScan(path string, plan ScanPlan) error` plus `ScanPlan{DefaultURLs, InventedGroups}` and `InventedGroup{Name, Dir, URLs}` matching spec; existing `AppendURL` unchanged; URL dedup against all existing groups is silent (UI-free).
- [ ] CHK-022 Atomicity and comment preservation: `MergeScan` uses the same `atomicWrite` (temp+rename in same dir) as `AppendURL`; preserves original file mode (0644 if absent); comments preserved across merge; rename failure leaves original untouched.
- [ ] CHK-023 `hop config init` tip extension: `newConfigInitCmd`'s post-write stderr is the exact two lines from spec § "Tip line extension" (mentions `hop config scan <dir>` first, retains `$HOP_CONFIG` tip second).
- [ ] CHK-024 `git` lazy check: empty scan trees (zero `.git` discoveries) succeed without invoking `git`; missing `git` only surfaces when walk requires a `git remote` invocation; CLI-surface table updated with `hop config scan` row referencing the same message.

## Behavioral Correctness
<!-- Changed requirements behave as specified, not as before -->
- [ ] CHK-025 `hop config init` post-write stderr changes from one line to two lines; the new opening sentence references `hop config scan <dir>`; the existing `$HOP_CONFIG` tip is preserved verbatim as the suffix; no other init behavior changes.
- [ ] CHK-026 New yamled API contract: `MergeScan` and the render-only sibling produce identical bytes for a given `ScanPlan` against a given file (print = "what `--write` would produce"); both routes preserve comments through yaml.v3 round-trip; URL dedup is silent at the yamled layer (CLI surfaces user-visible notes).

## Removal Verification
<!-- None — this change is purely additive (no requirements deprecated). -->
- [x] CHK-027 **N/A**: change is purely additive — no spec requirements deprecated, no code paths removed.

## Scenario Coverage
<!-- Key GIVEN/WHEN/THEN scenarios from spec.md exercised -->
- [ ] CHK-028 Cobra registration: `hop config --help` lists `scan` alongside `init` and `where`; exit 0.
- [ ] CHK-029 Direct invocation reachability: `hop config scan ~/code` executes the scan `RunE`, not "unknown command".
- [ ] CHK-030 No directory argument: `hop config scan` (no positional) yields cobra's "accepts 1 arg(s), received 0" error and exit 2.
- [ ] CHK-031 Argument is a file: `hop config scan /tmp/notadir.txt` yields the not-a-directory stderr (verbatim user input) and exit 2.
- [ ] CHK-032 Argument does not exist: `hop config scan /no/such/path` yields the not-a-directory stderr and exit 2 (EvalSymlinks ENOENT path).
- [ ] CHK-033 Argument is a symlink to a directory: walk proceeds against the canonical EvalSymlinks target.
- [ ] CHK-034 Default depth: invocation without `--depth` uses depth 3.
- [ ] CHK-035 Invalid depth: `--depth 0` yields stderr `--depth must be >= 1.` and exit 2.
- [ ] CHK-036 No `hop.yaml` exists: scan-specific missing-config stderr; "run hop config init first" hint; exit 1; no walk performed.
- [ ] CHK-037 `$HOP_CONFIG` set to non-existent file: same scan-specific missing-config stderr and exit 1.
- [ ] CHK-038 Successful scan with zero repos: stderr `scanned <user-arg> (depth N), found 0 repos. Nothing to add.`; stdout shows existing `hop.yaml` unchanged; exit 0.
- [ ] CHK-039 Print mode is pipeable: `hop config scan ~/code > hop.yaml` captures only rendered YAML on stdout; summary stays on stderr.
- [ ] CHK-040 Caller injects fake `GitRunner` for tests: `Walk` invokes the injected runner; no actual `git` subprocess spawns; `Found`/`Skip` deterministic.
- [ ] CHK-041 Depth halts descent at registered repo: child dirs of a `.git`-containing dir are not visited.
- [ ] CHK-042 Depth limit excludes deeper repos: a repo at depth 4 is absent from `Found` under `--depth 3`.
- [ ] CHK-043 Worktree detection: `.git` regular file → `Skip{Reason: "worktree"}`, not in `Found`.
- [ ] CHK-044 Submodule detection (or no-descent equivalent): submodules under registered repos are not in `Found`; if rule 2 fires explicitly, `Skip.Reason == "submodule"`; either path is acceptable per spec assumption #17 — implementation choice documented in tests.
- [ ] CHK-045 Bare repo detection: `HEAD` + `config` + `objects/` + no `.git` → `Skip{Reason: "bare repo"}`, not in `Found`.
- [ ] CHK-046 Repo with no remote: empty `git remote` output → `Skip{Reason: "no remote"}`, not in `Found`.
- [ ] CHK-047 Repo with origin remote: `origin` selected over other remotes; URL from `git remote get-url origin`.
- [ ] CHK-048 Repo with non-origin first remote: first remote line selected when `origin` absent; URL from `git remote get-url <first>`.
- [ ] CHK-049 Symlink loop: cyclic symlinks terminate via `(dev, inode)` dedup; no infinite descent; no `Skip` entry emitted.
- [ ] CHK-050 Same repo via two paths: `Found.Path` is canonical; repo registered exactly once.
- [ ] CHK-051 `git remote` times out: per-invocation 5s `context.WithTimeout` triggers; error propagates as walk-level error; no partial output; exit 1.
- [ ] CHK-052 Path matches convention exactly: URL placed in `default` group with no per-repo `dir:`.
- [ ] CHK-053 Path differs from convention: URL placed in invented group keyed off parent dir.
- [ ] CHK-054 Slugify scenarios: plain alphabetic basename, mixed case + symbols (`My Stuff!` → `my-stuff`), numeric leading char (`9-experiments` → `g9-experiments`), pathological empty-after-trim → CLI `skip:` stderr.
- [ ] CHK-055 Two distinct parent dirs, distinct names: each gets its own invented group; repos placed under their parent's group.
- [ ] CHK-056 Group dir rendering: under-HOME parent renders as `~/...`; outside-HOME parent renders as absolute path verbatim.
- [ ] CHK-057 Conflict reuse on dir match: existing group with same slug + same canonical dir → URLs appended; no `note:` line.
- [ ] CHK-058 Conflict suffix on dir mismatch: existing group with same slug + different dir → new group named `<slug>-2` with new dir; `note:` line on stderr.
- [ ] CHK-059 Print mode preserves comments: head comment + inline comments reproduced on stdout; `hop.yaml` on disk unchanged.
- [ ] CHK-060 Write mode preserves comments and is atomic: temp+rename in same dir; comments preserved; stdout empty; stderr ends with `wrote: <path>`.
- [ ] CHK-061 Header content: stdout's first two lines match the spec's header comment with today's UTC date and `(UTC)` suffix.
- [ ] CHK-062 Mixed existing and invented ordering: `vendor`, `default`, `experiments` rendered in that order when source had `vendor`+`default` and scan invented `experiments`.
- [ ] CHK-063 Invented groups sorted: scan inventing `zebra`/`alpha`/`mango` renders them as `alpha`/`mango`/`zebra` after existing groups and `default`.
- [ ] CHK-064 Print-mode summary with mixed counts: stderr block matches the spec's structure (header + indented breakdown + trailing `Run with --write to merge into <path>.`).
- [ ] CHK-065 Write-mode summary: shows `matched convention (default): 5 (3 new, 2 already registered)` style breakdown; ends with `wrote: <path>`.
- [ ] CHK-066 `MergeScan` default group does not exist: a new `default` flat group is appended; existing groups unchanged.
- [ ] CHK-067 `MergeScan` URL already registered elsewhere: silently skipped at yamled layer (CLI surfaces dedup notes if needed).
- [ ] CHK-068 `MergeScan` invented group renders as `{ dir, urls }` map shape; atomic write (temp+rename in same dir).
- [ ] CHK-069 `MergeScan` rename failure preserves original; returns non-nil error.
- [ ] CHK-070 Init followed by scan tip: `hop config init` stdout shows `Created <path>`; stderr shows the exact two-line tip; exit 0.
- [ ] CHK-071 Empty tree, git missing: zero `.git` discoveries → success (exit 0); no `git is not installed` message.
- [ ] CHK-072 Non-empty tree, git missing: `.git` candidate present → `hop: git is not installed.` stderr and exit 1.

## Edge Cases & Error Handling
<!-- Error states, boundary conditions, failure modes -->
- [ ] CHK-073 Missing `hop.yaml`: scan exits 1 with the two-line scan-specific stderr; no walk, no `git`.
- [ ] CHK-074 No remote on a `.git` dir: `Skip{Reason: "no remote"}` emitted; not in `Found`; counted in summary's `no remote` skip total.
- [ ] CHK-075 Bare repo: detected via stat heuristic; `Skip{Reason: "bare repo"}` emitted; not descended.
- [ ] CHK-076 Worktree: `.git` as regular file detected; `Skip{Reason: "worktree"}`; not descended.
- [ ] CHK-077 Submodule: not present in `Found` (whether via explicit rule 2 or via no-descent invariant); behavior documented in tests.
- [ ] CHK-078 Write permission failure on `hop.yaml`: write-mode error surfaces with exit 1; original file untouched (atomic rename's pre-conditions guarantee this).
- [ ] CHK-079 `--depth < 1`: rejected at flag parse / `RunE` start with exit 2 and exact stderr text; no walk occurs.
- [ ] CHK-080 Slugify-empty (pathological parent base like `___` or `///`): CLI emits `skip: <Path>: cannot derive group name from parent dir '<base>'` to stderr; repo not added; counted as skipped; other repos unaffected.
- [ ] CHK-081 Group-name collision (existing slug, differing dir): suffixed with smallest non-colliding `-N` (≥2); `note:` stderr line emitted; suffix increments through `-3`, `-4`, … as needed.
- [ ] CHK-082 Symlink loop: `(dev, inode)` dedup terminates the cycle; silent (no `Skip` entry); walk completes successfully.
- [ ] CHK-083 `git` missing on PATH: lazy check — only fires when a `.git` dir requires invocation; stderr `hop: git is not installed.` exit 1; scan halts (does not continue to other candidates).
- [ ] CHK-084 Invalid `<dir>` (non-directory file, non-existent path, EvalSymlinks failure): exit 2 with `'<dir>' is not a directory.` stderr; no `git` subprocess spawned.

## Code Quality
<!-- Per fab/project/code-quality.md (Principles + Anti-Patterns) plus baseline items -->
- [ ] CHK-085 Principle — Readability over cleverness: walker, classifier, slugify, and conflict-resolution code prefer straightforward control flow (linear DFS loop, ordered classifier rules, named slugify steps) over compact tricks; intent reads at the top level.
- [ ] CHK-086 Principle — Follow existing patterns: `internal/scan` mirrors `internal/yamled`/`internal/update` package shape; `RunCapture` (or `Run` extension) follows existing `internal/proc` style; cobra factory `newConfigScanCmd` mirrors `newConfigInitCmd`/`newConfigWhereCmd`; CLI exit conventions match `hop clone`.
- [ ] CHK-087 Principle — Composition over inheritance: classifier and renderer composed from small helpers (e.g., `isWorktree`, `isBareRepo`, `selectRemote`, `slugify`) rather than monolithic methods; `ScanPlan` data structure carries cross-layer state instead of inheritance hierarchies.
- [ ] CHK-088 Anti-pattern — God functions: `Walk`, `RunE`, and `MergeScan` each stay focused (>50 lines justified by table-driven structure if it occurs); classifier rules, slugify, conflict resolution, and rendering are extracted into named helpers, not inlined.
- [ ] CHK-089 Anti-pattern — Duplicating utilities: convention check reuses `repos.DeriveOrg`/`DeriveName`/`ExpandDir`; `git` invocation reuses `internal/proc` (no ad-hoc `exec.Command` calls); render path reuses yamled's existing `atomicWrite`; HOME substitution reuses any existing helper rather than reinventing.
- [ ] CHK-090 Anti-pattern — Magic strings/numbers: closed `Skip.Reason` set defined as named constants; default depth `3`, timeout `5*time.Second`, suffix-start `2`, regex `^[a-z][a-z0-9_-]*$` are referenced via named identifiers; stderr message strings live as constants or single-call-site literals (not duplicated).
- [ ] CHK-091 Pattern consistency (baseline): new code follows naming and structural patterns of surrounding code (lowercase package name `scan`, exported types in PascalCase, error messages prefixed `hop config scan:` matching `hop clone:` style).
- [ ] CHK-092 No unnecessary duplication (baseline): print and write modes share a single render primitive (`RenderScan`/`MergeScan`); CLI does not reimplement YAML serialization; no copy-pasted classifier branches.

## Security
<!-- Per Constitution I "Security First" and spec § Security -->
- [ ] CHK-093 Input validation on `<dir>` precedes execution: `filepath.Clean` → `filepath.EvalSymlinks` → `os.Stat` directory check completes before any `git` subprocess is spawned (auditable via test seam).
- [ ] CHK-094 All `git` calls go through `internal/proc` with `exec.CommandContext` and explicit argument slices; no `git` argument passed via `sh -c` or string concatenation; `<dir>` and discovered paths passed as `cmd.Dir` or via `git -C <path>` as a single argv element.
- [ ] CHK-095 Per-invocation 5s context timeout: every `git` call wraps the parent `ctx` with `context.WithTimeout(ctx, 5*time.Second)`; timeouts surface as walk-level errors with exit 1.
- [ ] CHK-096 No shell interpolation: paths containing shell metacharacters (`/tmp/test;rm/foo/.git`) are passed as a single argv element; `git` either succeeds or fails on the literal path; no command injection.
- [ ] CHK-097 Permissive on URL contents: URLs from `git remote get-url` are emitted into YAML verbatim (yaml.v3 quotes as needed); not regex-validated, not shell-expanded, not re-interpreted on write or load.

## Notes

- Check items as you review: `- [x]`
- All items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] CHK-NNN **N/A**: {reason}`
