# config — memory

| File | Topic |
|---|---|
| [search-order](search-order.md) | `$REPOS_YAML` → `$XDG_CONFIG_HOME/repo/repos.yaml` → `$HOME/.config/repo/repos.yaml`, hard-error semantics |
| [yaml-schema](yaml-schema.md) | Directory-keyed map of git URLs; Repo field derivation |
| [init-bootstrap](init-bootstrap.md) | `repo config init` write target, mode 0644, embedded starter |
